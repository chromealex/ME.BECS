using UnityEngine;
using ME.BECS;
using ME.BECS.Network;
using Unity.Burst;

namespace {{PROJECT_NAME}} {
    
    public class {{SCRIPT_NAME_SHORT}}World {
        public static readonly SharedStatic<World> world = SharedStatic<World>.GetOrCreate<{{SCRIPT_NAME_SHORT}}World>();
        public static World World => world.Data;
    }
    
    public class {{SCRIPT_NAME}} : NetworkWorldInitializer {

        private static NetworkModule netModule;

        protected override void Awake() {
            
            base.Awake();
            {{SCRIPT_NAME_SHORT}}World.world.Data = this.world;
            netModule = this.GetModule<NetworkModule>();
            
        }

        public static NetworkModule GetNetworkModule() {
            return netModule;
        }

    }
    
}